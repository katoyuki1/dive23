<h1>回答を修正してください</h1>

<%= form_for(@answer, url: question_answer_path) do |f| %>
    <%= f.text_area :content, class: "form-control", placeholder: 'コメントを入力' %>
    <%= f.submit 'コメントを編集', class: 'btn btn-primary' %>
<% end %>

<%= link_to '戻る', question_path(@answer.question) %>
